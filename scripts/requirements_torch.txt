# requirements_torch.txt — PyTorch environment (venv_torch)
# Used by: all scripts EXCEPT convert_model.py
#
# NOTE: PyTorch (torch, torchvision, torchaudio) is NOT listed here.
# It is installed BEFORE this file with --index-url for CUDA support.
# Pip will see torch is already satisfied and won't pull CPU-only from PyPI.
#
# Install: venv_torch\Scripts\pip install -r scripts\requirements_torch.txt

# HuggingFace (shared with training + MMS TTS)
transformers>=4.30.0
safetensors>=0.4.0
numpy>=1.24.0

# Audio tools
yt-dlp>=2024.1.0
pydub>=0.25.1
sounddevice>=0.4.6
soundfile>=0.12.1

# MMS TTS (Meta Massively Multilingual Speech)
ttsmms>=0.7

# Training pipeline
accelerate>=0.25.0
datasets>=2.14.0

# Video OCR (reads on-screen text from lesson videos)
opencv-python>=4.8.0
easyocr>=1.7.0

# Whisper ASR (auto-labels audio clips)
openai-whisper>=20231117

# Visualization
matplotlib>=3.7.0
tensorboard>=2.14.0
