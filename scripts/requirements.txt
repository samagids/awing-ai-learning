# requirements.txt — Single venv for all Awing AI Learning scripts
# Used by: install_dependencies.bat v3.0.0
#
# NOTE: PyTorch (torch, torchvision) is NOT listed here.
# It is installed separately with --index-url for CUDA support.
# See install_dependencies.bat step 7.
#
# Install: venv\Scripts\pip install -r scripts\requirements.txt

# Edge TTS (neural voice audio generation — PRIMARY audio source)
edge-tts>=6.1.0

# Image processing
Pillow>=10.0.0

# Audio processing
pydub>=0.25.1

# Image generation (SDXL Turbo on local GPU)
diffusers>=0.25.0
transformers>=4.30.0
accelerate>=0.25.0

# Whisper ASR (optional — used by apply_contributions.py to transcribe
# developer voice recordings into a speakable_override that guides Edge TTS.
# Without it, apply_contributions falls back to pronunciationGuide or raw text.)
openai-whisper>=20231117

# Microphone recording (used by scripts/record_audio.py)
sounddevice>=0.4.6
soundfile>=0.12.1
